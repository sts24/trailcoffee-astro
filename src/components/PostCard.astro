---

const { post } = Astro.props;
console.log(post);
---
<article class="post-card">
	<header class="post-card-header" style="border-top: 4px solid ">
		<h2><a href="/places/">{ post.data.tags }</a></h2>
	</header>

	<a href="" class="post-card-img" aria-label="{ post.data.title }">
		<!-- <img srcset="{% imgPath post.data.image, '400', '400', 'fill' %} 1x, {% imgPath post.data.image, '800', '800', 'fill' %} 2x" width="400" height="400" alt="{{ post.data.title }}" /> -->
	</a>
	<div class="post-card-meta">
		<div class="post-title"><a href="">{ post.data.title }</a></div>
		<p class="excerpt">{ post.data.excerpt }</p>
	</div>
</article>